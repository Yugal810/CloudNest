# Render Blueprint — SPA rewrite so /dashboard, /login, etc. work on refresh
services:
  - type: web
    name: cloudnest-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
